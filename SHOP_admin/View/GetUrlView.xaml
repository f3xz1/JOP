<Window x:Class="SHOP_admin.View.GetUrlView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SHOP_admin.View"
        mc:Ignorable="d"
        Title="GetUrlView" Height="150" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Label Content="Input the picture url" Margin="10" Grid.Row="0" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Margin="10" Text="{Binding Url}"/>
        <Button Grid.Row="2" Margin="10" Content="Done"/>
    </Grid>
</Window>
